# API Configuration — the FastAPI backend this frontend actually talks to.
NEXT_PUBLIC_API_URL=http://localhost:8000

# For production:
# NEXT_PUBLIC_API_URL=https://your-api-domain.com

# NOTE: @supabase/supabase-js is listed in package.json but is not used anywhere in
# this app's code — the app talks to the FastAPI backend above, not Supabase directly.
# Remove the dependency or wire up real Supabase usage; don't leave both implied at
# once, since it's misleading about what the app actually depends on.
