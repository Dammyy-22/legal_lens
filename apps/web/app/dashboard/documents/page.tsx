import { ComingSoonPage } from '@/components/ComingSoonPage'

export default function DocumentsPage() {
  return (
    <ComingSoonPage
      mark="⎘"
      title="My documents"
      description="Upload your own documents and ask questions about them, with answers referenced to the exact page or clause. This needs secure upload handling and document processing that hasn't been built yet."
      detail="Backend: upload pipeline + document Q&A (not started)"
    />
  )
}
